name: Test
description: Test stage
runs:
  using: composite
  steps:
    - name: Lint
      shell: bash
      run: yarn nx affected:lint

    - name: Test
      shell: bash
      run: yarn nx affected:test
